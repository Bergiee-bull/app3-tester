{
  "name": "app3-tester",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js && node scripts/validate-data.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1"
  }
}
